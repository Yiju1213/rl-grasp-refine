# 预留的 predictor 拆分配置文件。
# 当前脚本虽然会把它读进 experiment bundle，但并不会直接消费它；
# 当前真正生效的是 configs/perception/perception.yaml 里的 predictor 段。

# 期望的 predictor 类型。
# 当前代码没有直接读取这个字段；factory 里固定实例化 StabilityHead。
type: stability_head

# 期望的 predictor 隐藏层宽度。
# 当前这个文件未直接生效；真实生效值来自 perception.yaml 的 predictor.hidden_dim。
hidden_dim: 64
